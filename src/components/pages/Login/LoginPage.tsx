import LoginFormContainer from "components/Login/LoginFormContainer";

const LoginPage = () => {
  return (
    <div>
      <h1>Login</h1>
      <LoginFormContainer />
    </div>
  );
};

export default LoginPage;
